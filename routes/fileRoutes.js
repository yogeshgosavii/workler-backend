import { Router } from 'express';
const router = Router();
// import { uploadImages } from '../controllers/fileController.js'; 


// router.route('/upload-image').post(uploadImages);

export default router;
